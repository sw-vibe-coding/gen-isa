Built gen-isa scaffolder Rust binary (step 4). Given --slug / --display-name / --type-name / --out / --framework-path, emits the 5-crate quintet (sw-{slug}-{isa,target,codegen,asm,emulator}) with Cargo.toml (deps path-wired), LICENSE/COPYRIGHT/.gitignore, README.md (per-role), src/lib.rs (re-exports + marker type matching the role), per-role module stubs (5 stubs for -isa; 3 for -target plus docs/abi.md; 4 for -codegen; 3 each for -asm and -emulator), tests/smoke.rs. CLI via clap-derive with subcommand 'scaffold' and --dry-run / --force flags. Filesystem trait abstracts disk access; InMemoryFs is the test fixture. 8 integration tests pass (positive scaffolding, slug validation, existing-file rejection without --force, --force overwrite, dry-run listing, per-role contents). Acceptance: scaffolded sw-accept-* into a tempdir with --framework-path $HOME/github/sw-langtools; all 5 generated crates compile via cargo build. cargo build/test/clippy -D warnings/fmt --check on gen-isa itself: clean.