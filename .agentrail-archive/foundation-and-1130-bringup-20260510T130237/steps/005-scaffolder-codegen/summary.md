Step 5 (scaffolder-codegen) shipped. Added --spec <PATH> to gen-isa: parses TOML ISA specs (src/spec.rs ~280 lines, typed structs + validation rules covering format references / opcode uniqueness / bit coverage / discriminator presence) and emits real Rust for the -isa crate's mechanical bits via src/emit.rs (~520 lines). Generates: opcode.rs (enum + mnemonic + formats + try_from_value + Mnemonic impl), register.rs (enum + class + RegisterId), branch.rs (constants + can_short_branch), encode.rs / decode.rs (working bit-field math for bit_fields style; stubs for rom_table), lib.rs (Architecture impl + Address newtype + Instruction enum), tests/roundtrip.rs (curated case per format, or #[ignore] for rom_table). 11 integration tests, clippy-D-warnings clean, fmt clean. End-to-end acceptance: scaffolded sw-testibm-isa from ibm1130.toml -- builds + 2 roundtrip tests pass; scaffolded sw-testcor-isa from cor24.toml (rom_table) -- builds + 1 ignored roundtrip + smoke passes.