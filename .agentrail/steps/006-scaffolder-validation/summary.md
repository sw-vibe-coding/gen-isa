Step 6 (scaffolder-validation) shipped. Cross-checked the smart scaffolder against the existing sw-embed/sw-cor24-isa. Generated sw-cor24-isa from cor24.toml into a tempdir; built+tested clean (rom_table roundtrip #[ignore]'d as designed). Per-module verdicts in docs/cor24-validation.md (~190 lines): opcode (Match for spec subset; design diff InstructionFormat-vs-formats-slice acceptable), register (Match semantic; const-array vs enum design deferred to phase-6 retrofit), branch (Match after abs_diff fix), lib (forward-looking pre-retrofit divergence expected), encode/decode (rom_table opt-out works). Bottom line: scaffolder is faithful enough to proceed to step 7. Caught one real bug during validation: generator's can_short_branch used directional comparison (false if from > to within range); patched to abs_diff matching the existing crate. Three spec extensions deferred (non-blocking): register aliases, optional InstructionFormat enum emission, custom mnemonic-overload models -- none needed for IBM 1130 bring-up.