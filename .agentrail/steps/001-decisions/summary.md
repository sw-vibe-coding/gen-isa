Step 1 complete (reframed mid-step per user redirect). docs/forth-on-1130-decisions.md captures the saga's *forced deltas* from Moore's 1968 design rather than treating architectural questions (threading, primitive set, etc.) as decisions we make. We are recreating Moore's FORTH, not designing one. Seven deltas catalogued: ASCII source (temporary, EBCDIC-saga later), XR3 reservation (permanent, ABI-driven), free-form columns + semicolon comments (trivial), BSC mask field (saga step 3 closes), missing asm directives (saga step 4 adds the in-scope subset; defers LIBF/CALL/ENT/EXT/ISS/ILS), block I/O (conditional on what Moore's source uses).