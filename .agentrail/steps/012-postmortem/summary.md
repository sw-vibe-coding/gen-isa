Wrote docs/postmortem-1130-bringup.md (10 sections) capturing the 1130 bring-up saga's lessons: ABI revision driven by historical-listings research (XR3 is the LIBF transfer-vector base, not a FP); BSC long-form mask field gap; slot-based register-allocator-free codegen as intentional debt; asm syntax decisions (colon labels, I/L lookahead); emulator scope (pinned BSC semantics, single-device XIO). Includes 8 concrete lessons for ISA #2 plus a future-work catalogue. Cross-linked from gen-isa/docs/status.md.