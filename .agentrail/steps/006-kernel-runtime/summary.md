Saga step 6 complete. Translated kernel runs on emulator for 200 steps without crashing. tests/kernel_runtime.rs verifies: kernel assembles + load-bearing symbols resolve >= 0x900; after 25 steps XR2 = STACK+1 and XR1 = A (START's first register loads work); 200 steps without decode-error / divide-by-zero / unsupported-opcode. historical/forth68/RUNTIME-STATUS.md captures what works, what hits a stub, open questions. Two toolchain bugs surfaced and fixed: LDX semantics in the emulator (was dereferencing the address operand instead of taking it as immediate value) and the asm's symbolic-operand auto-promote-to-long (added mid-step-5). The XR3-use-preserved deviation from decisions Sec 2.2 (LC.9 in TRANSLATION-LOG) is confirmed correct: kernel runs in isolation with no cross-contamination.