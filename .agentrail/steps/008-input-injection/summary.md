Saga step 8 complete. Three new runtime tests in tests/kernel_parses.rs all pass: kernel runs 1000 steps without decode-error (5x the step-6 ceiling); IAR migrates out of START into the primitive area (FORTH_RTN's NEXT/DO loop is working); input-injection scaffold writes EBCDIC bytes into SECT_BASE and points workspace[-3] at it (no crash, but workspace[0] not yet populated -- the byte-vs-word arithmetic from LC.5 still has kernel-initialised pointers in word-address form). 8 tests pass total. RUNTIME-STATUS.md updated.