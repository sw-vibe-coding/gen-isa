Saga step 3 complete: BSC/BSI long-form condition mask field is now first-class across the toolchain. Changes spanned 5 repos (gen-isa spec, sw-ibm1130-isa, -asm, -codegen, -emulator). Mask bit assignments reconciled to Moore's authoritative 1968 listing (0x04=E, 0x08=+, 0x10=-, 0x20=Z, 0x40=C); previous emulator stand-in convention retired. Demos rewritten with direct masked branches (1 instruction per logical conditional branch instead of the skip-and-jump pair). All four affected sibling crates build/test/clippy -D warnings/fmt --check clean. Survey doc Sec 4 marks the gap closed.